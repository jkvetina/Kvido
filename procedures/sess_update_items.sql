CREATE OR REPLACE PROCEDURE sess_update_items --(
--    in_item1_value := NULL,
--    in_item2_value := NULL,
--    in_item3_value := NULL
--)
AS
    --req                 CONSTANT VARCHAR2(4000) := SUBSTR(sess.get_request(), 1, 4000);
BEGIN
    -- set global items
    --apex.set_item('NAME1', in_item1_value);
    --
    NULL;
END;
/
